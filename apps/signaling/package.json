{
  "name": "@industrial/signaling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "prepare": "pnpm run build",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run",
    "lint": "eslint src test --ext .ts"
  },
  "dependencies": {
    "express": "^5.1.0",
    "nanoid": "^5.1.6",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^25.6.0",
    "tsx": "^4.20.3"
  }
}