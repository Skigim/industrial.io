{
  "name": "@industrial/net-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "test": "vitest run && pnpm run test:package",
    "test:package": "pnpm run build && node ./test/package-smoke.mjs",
    "lint": "eslint src test --ext .ts"
  },
  "dependencies": {
    "msgpackr": "^1.11.2",
    "zod": "^3.24.2"
  }
}